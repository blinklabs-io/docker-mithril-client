# Generated automatically by org-governance-bot. Do not edit manually.
name: "publish"

on:
  push:
    branches:
      - main
    tags:
      - v*.*.*

permissions:
  contents: write
  packages: write

jobs:
  orchestrate:
    uses: blinklabs-io/actions/.github/workflows/reuseable-publish-docker-multiarch.yml@feat-use-actions-reuseable-workflows
    secrets:
      docker-password: ${{ secrets.DOCKER_PASSWORD }}
    with:
      description: Mithril client built from source on Debian
      docker-image: blinklabs/mithril-client
      ghcr-image: blinklabs-io/mithril-client
